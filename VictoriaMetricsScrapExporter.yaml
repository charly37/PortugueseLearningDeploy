apiVersion: v1
kind: ConfigMap
metadata:
  name: vm-scrape-config
  namespace: logging
data:
  scrape.yml: |
    scrape_configs:
      - job_name: node-exporter
        static_configs:
          - targets: ['node-exporter-prometheus-node-exporter:9100']